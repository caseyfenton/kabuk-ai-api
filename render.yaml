services:
  - type: web
    name: kabuk-ai-api
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn webhook_server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
